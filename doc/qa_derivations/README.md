# QA blind derivations

QA-only workspace: expected outcomes derived from **spec + assets + human oracle** — never from solver source or `calculate_next_move`.

See [QA_ORACLE_AUDIT.md](../QA_ORACLE_AUDIT.md).

One file per manifest `id`, e.g. `tier4-phantom-synthetics.md`, `t6-seq22-30-human.md`.
